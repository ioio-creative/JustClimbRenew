<Window x:Class="JustClimbTrial.Views.Windows.Playground"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JustClimbTrial.Views.Windows"
        mc:Ignorable="d"
        Title="Playground" Name="PlaygroundWindow"
        Height="720" Width="1280" Background="White">

    <Viewbox Name="PlaygroundView" RenderTransformOrigin="0.5, 0.5" Stretch="Uniform"
              Margin="0,0,-8,-31">
        <Grid>
            <MediaElement x:Name="PlaygroundMedia" ScrubbingEnabled="False" LoadedBehavior="Manual" UnloadedBehavior="Close" Stretch="None"                
                              Loaded="PlaygroundMedia_Loaded"
                      MediaOpened="PlaygroundMedia_MediaOpened" 
                      MediaEnded="PlaygroundMedia_MediaEnded">
                <MediaElement.LayoutTransform>
                    <ScaleTransform ScaleX="-1"/>
                </MediaElement.LayoutTransform>
            </MediaElement>
            <Image Name="PlaygroundCamera" Stretch="Uniform">
            </Image>
            <Canvas Name="PlaygroundCanvas" Width="{Binding ActualWidth, ElementName=PlaygroundCamera, Mode=OneWay}" Height="{Binding ActualHeight, ElementName=playgroundCamera, Mode=OneWay}"
                        Background="Transparent">
<<<<<<< HEAD
            </Canvas>
            <MediaElement x:Name="PlaybackMedia" ScrubbingEnabled ="True"
                              LoadedBehavior="Manual" UnloadedBehavior="Close" Stretch="Uniform"
                              MediaOpened="PlaybackMedia_MediaOpened" Loaded="PlaybackMedia_Loaded"/>
        </Grid>
        <Viewbox.RenderTransform>
            <ScaleTransform ScaleX="-1"/>
        </Viewbox.RenderTransform>
    </Viewbox>

                </Canvas>
                <MediaElement x:Name="PlaybackMedia" ScrubbingEnabled ="True"
                              LoadedBehavior="Manual" UnloadedBehavior="Close" Stretch="Uniform"
                              MediaOpened="PlaybackMedia_MediaOpened" Loaded="PlaybackMedia_Loaded"/>
            </Grid>
            <Viewbox.RenderTransform>
                <ScaleTransform ScaleX="-1"/>
            </Viewbox.RenderTransform>
        </Viewbox>
        
        <!--<Viewbox>
            <MediaElement x:Name="PlaybackMedia" ScrubbingEnabled ="True"
                          LoadedBehavior="Manual" UnloadedBehavior="Close" Stretch="Uniform"
                          Width="{Binding RelativeSource={RelativeSource Self},Path=Parent.ActualWidth}" Height="{Binding RelativeSource={RelativeSource Self},Path=Parent.ActualHeight}"
                          MediaOpened="PlaybackMedia_MediaOpened" Loaded="PlaybackMedia_Loaded"/>
        </Viewbox>-->




</Window>

